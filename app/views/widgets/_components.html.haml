%strong Components
%br
%ul#components
  - @widget_components.each do |widget_component|
    %li= link_to widget_component.component.name, component_path(widget_component.component)
    %ul#parts
      - widget_component.component.parts.each do |part|
        %li= link_to "#{part.name} - #{widget_component.quantity} units required", part_path(part)