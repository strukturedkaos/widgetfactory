%strong Components
%br
%ul#components
  - @widget.components.each do |component|
    %li= link_to component.name, component_path(component)
    %ul#parts
      - component.parts.each do |part|
        %li= link_to part.name, part_path(part)